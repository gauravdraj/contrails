{
  "name": "contrails-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy for Contrails — ADS-B, route, schedule, and photo APIs",
  "scripts": {
    "test": "node --test src/*.test.js",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
